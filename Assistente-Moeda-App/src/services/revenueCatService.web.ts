import { Platform } from 'react-native';

export async function initRevenueCat() {
  // Web Mockup
}

export async function fetchOfferings() {
  return { current: null, all: {} };
}

export async function purchasePackage(rcPackage: any): Promise<any | null> {
  return null;
}
