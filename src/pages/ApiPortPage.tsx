/**
 * Page wrapper for the Universal API Port tool.
 * Mounted at /laboratorio/api-port in the main router.
 */

import { UniversalApiPort } from '../tools/universal-api-port';

export function ApiPortPage() {
  return <UniversalApiPort />;
}
