// Public surface of the Logic Defense game module.
// Pages and routes should import exclusively from this barrel —
// never from the internal engine/, hooks/, or components/ sub-trees.
export { GamePage } from './components/GamePage'
